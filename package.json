{
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix frontend\"",
    "dev:server": "npm run dev --prefix server",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "supabase": "^2.98.2"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "pg": "^8.20.0",
    "ws": "^8.20.1"
  }
}
